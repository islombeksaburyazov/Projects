if(!matchMedia('(prefers-reduced-motion: reduce)').matches&&matchMedia('(pointer: fine)').matches){document.querySelectorAll('.tilt').forEach(el=>{el.addEventListener('pointermove',event=>{const r=el.getBoundingClientRect();const x=(event.clientX-r.left)/r.width-.5;const y=(event.clientY-r.top)/r.height-.5;el.style.transform=`perspective(1100px) rotateY(${x*7}deg) rotateX(${-y*6}deg) translateY(-3px)`});el.addEventListener('pointerleave',()=>{el.style.transform=''})})}
